This directory where I store scripts to inference and evalutate models.

### !!! NOTES
You should create a PAT on GitHub then flag `--header="Authorization: token YOUR_TOKEN"` to wget command in scripts [Setup](tools/deploy-model/setup.sh).