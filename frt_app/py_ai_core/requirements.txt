# FRTApp Python AI Core Requirements
# Version: 1.0
# Description: Dependencies for Food Recognition Tracking AI pipeline
# SDD Version: 1.1.0
# NOTE: Uses tflite-runtime per SDD requirement (NO Ultralytics - Pi 4B constraint)

# =====================================================================
# CORE VISION & AI DEPENDENCIES
# =====================================================================
# Headless OpenCV - optimized for Embedded/Headless systems (Raspberry Pi 4)
# - No GUI dependencies (X11, GTK)
# - Lightweight for ARM aarch64
# - V4L2 backend for USB camera integration
opencv-python-headless>=4.8.0

# NumPy - numerical computing for tensor operations
# Used in: ImagePreprocessor, YoloTfliteEngine for matrix operations
numpy>=1.24.0

# TFLite Runtime - lightweight TensorFlow Lite inference engine
# NOTE: No wheel for Python 3.13 on ARM64. Using C backend (libtflite_reader.so) instead.
# See frt_app/c_tflite_reader/ for the C implementation.
# If you need Python-only fallback, install manually:
#   pip install tflite-runtime
# tflite-runtime>=2.14.0

# Pillow - image processing utilities for preprocessing
Pillow>=9.0.0

# =====================================================================
# IPC & SYSTEM INTEGRATION
# =====================================================================
# D-Bus communication - inter-process message passing
# Provided by system package: apt install python3-sdbus
# Used for: Door events from SensorDaemon, results to DBDaemon
# sdbus-python>=0.12.0

# Python-systemd - integration with systemd service management
# Provided by system package: apt install python3-systemd
# python-systemd>=235

# =====================================================================
# LOGGING & MONITORING
# =====================================================================
# Loguru - advanced structured logging with file rotation
loguru>=0.7.0

# psutil - system monitoring for crash recovery
# Used in: YoloTfliteEngine.handle_tensor_allocation_error()
psutil>=5.9.0