#ifndef UVC_STILL_MAGIC_HPP
#define UVC_STILL_MAGIC_HPP

/**
 * @brief Still image format magic from the custom uvc driver
 * 
 * Used for still image capture support in the custom uvc kernel module.
 */
#define UVC_STILL_MAGIC 0xF03E5335

#endif // UVC_STILL_MAGIC_HPP
